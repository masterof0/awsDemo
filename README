From a connected browser, access the UI using http://<ip>:5000/

Service is set to start on boot. If, for some reason you need to start/stop it manually, run the following command:
sudo initctl (start/stop) codeaws

Logs can be found at /var/logs/upstart/codeaws.log

